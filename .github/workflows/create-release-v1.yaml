on:
  workflow_dispatch:
    inputs:
      COMMITID:
        required: true
        type: string
      TAG:
        required: true
        type: string         
name: Create Release V1

jobs:
  build:
    name: Create Release v1
    runs-on: ubuntu-latest
    permissions:
     contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
         ref: 'artifacts'
      - name: Create Release
        id: create_release
        uses: ncipollo/release-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        with:
          commit : 036a0d5ec8ceacbba66fc8f6eee73bc66651583e
          name: Release ${{ inputs.TAG }}
          tag: ${{ inputs.TAG }}
          draft: false
          prerelease: false
          token: ${{ env.GITHUB_TOKEN }}
